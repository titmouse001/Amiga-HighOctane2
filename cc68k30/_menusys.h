#ifndef _MENU_H_
#define _MENU_H_

#define TRUE	(1)
#define FALSE 	(0)
#define max(a,b)  (((a) > (b)) ? (a) : (b))
#define min(a,b)  (((a) < (b)) ? (a) : (b))
#define abs(i) ((i < 0L) ? -i : i)

#endif